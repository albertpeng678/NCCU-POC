# Q&A 2.5 JSON 強化 + 平滑串流打字機 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 留 `gemini-2.5-flash` 串流(穩、快),用後端 JSON 三層強化(不漏鷹架)+ 前端定速緩衝(平滑、表格 snap-in)把「漏 JSON / 一坨一坨跳 / 表格 raw」三個問題壓掉。

**Architecture:** 後端 `parse_qa_response` 補 `json_repair`+絕不漏鷹架、`stream_answer` 加「JSON-first 守門」(`_partial_answer` 增量抽乾淨值);前端 `/qa/stream` token 改餵 `createTypewriter`(定速緩衝 + healed markdown + backlog 自適應),`QA_MODE` 預設翻 `stream`(3.5 留 `replay` 選項)。

**Tech Stack:** Python 3.11 + FastAPI + google-genai 1.68 + json-repair;原生 JS(marked + DOMPurify);pytest + Node `--test` + Playwright。

**紀律:** 全程 Karpathy(外科手術式最小 diff、明列假設、可驗證成功標準);每 task 結尾 commit,訊息加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。分支 `feat/qa-hardening-smooth-stream`(已建)。

**前置假設(已驗證):** `json_repair`、`_strip_fences`、`finalize_qa_answer`、`stripInProgressTable`、`createTypewriter`、`safeMarkdownPrefix`、`renderSafeMarkdown` 皆已 commit 於本分支;`_partial_answer` 於 stash(本計畫重新實作,不 pop stash 以免帶回 3.5 wiring)。

---

### Task 1：`parse_qa_response` 補容錯 + 絕不漏鷹架(A1)

**Files:**
- Modify: `backend/qa.py`(`parse_qa_response` 的最終 fallback)
- Test: `tests/backend/test_qa_response_hardening.py`

- [ ] **Step 1：寫失敗測試**

Create `tests/backend/test_qa_response_hardening.py`:
```python
# tests/backend/test_qa_response_hardening.py
"""parse_qa_response 強化：json_repair 救壞 JSON；全失敗也絕不回傳含鷹架原文。"""
from backend.qa import parse_qa_response


def test_repairs_literal_newline_in_json_string():
    # 表格塞 JSON 字串常見：字面換行 → 標準 json.loads 失敗 → json_repair 救回
    raw = '```json\n{"answer": "第一行\n第二行", "followup_suggestions": ["q1"]}\n```'
    out = parse_qa_response(raw)
    assert "第一行" in out["answer"] and "第二行" in out["answer"]
    assert out["followup_suggestions"] == ["q1"]


def test_unrecoverable_never_leaks_scaffolding():
    raw = '```json\n{ 這完全不是合法 JSON ```'
    out = parse_qa_response(raw)
    assert "```" not in out["answer"]
    assert '{"answer"' not in out["answer"]
    assert "json" not in out["answer"].lower()
    assert out["followup_suggestions"] == []


def test_plain_prose_passthrough():
    out = parse_qa_response("這是一段沒有 JSON 的純文字回答。")
    assert out["answer"] == "這是一段沒有 JSON 的純文字回答。"
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_response_hardening.py -v`
Expected: FAIL（`test_repairs_literal_newline_in_json_string` 與 `test_unrecoverable_never_leaks_scaffolding` 失敗——目前 fallback 會回傳含鷹架原文）

- [ ] **Step 3：實作**

在 `backend/qa.py` 的 `parse_qa_response`，把最後的：
```python
    # Fallback: treat the whole response as the answer
    return {"answer": stripped, "followup_suggestions": []}
```
改為(在 fence/brace 之後、回傳前補 json_repair,再退 `_strip_fences`):
```python
    # json_repair 容錯救壞 JSON（字面換行/trailing comma/截斷/缺括號等）
    try:
        data = json_repair.loads(stripped)
        if isinstance(data, dict) and "answer" in data:
            return {
                "answer": str(data.get("answer", "")),
                "followup_suggestions": list(data.get("followup_suggestions", []) or []),
            }
    except Exception:
        pass

    # 全失敗：剝掉 ```json/``` 與外殼，**絕不回傳含 {"answer" 的原文**
    cleaned = _strip_fences(stripped)
    looks_json = cleaned.startswith("{") and '"answer"' in cleaned
    return {"answer": "" if looks_json else cleaned, "followup_suggestions": []}
```

- [ ] **Step 4：跑測試確認通過 + qa 全測不回歸**

Run: `python -m pytest tests/backend/test_qa_response_hardening.py tests/backend/test_qa.py -v`
Expected: PASS

- [ ] **Step 5：Commit**

```bash
git add backend/qa.py tests/backend/test_qa_response_hardening.py
git commit -m "fix(qa): parse_qa_response — json_repair fallback + never leak scaffolding"
```

---

### Task 2：`_partial_answer` 增量抽取 helper(A2 前置)

**Files:**
- Modify: `backend/qa.py`（新增純函式）
- Test: `tests/backend/test_qa_partial_answer.py`

- [ ] **Step 1：寫失敗測試**

Create `tests/backend/test_qa_partial_answer.py`:
```python
# tests/backend/test_qa_partial_answer.py
"""_partial_answer：從半截串流 JSON 容錯解出當前 answer 欄位值。"""
from backend.qa import _partial_answer


def test_partial_growing_json():
    assert _partial_answer('{"answer": "政大在') == "政大在"
    assert _partial_answer('{"answer": "政大在 114-2') == "政大在 114-2"


def test_complete_json():
    assert _partial_answer('{"answer": "完整答案", "followup_suggestions": []}') == "完整答案"


def test_empty_or_garbage():
    assert _partial_answer("") == ""
    assert _partial_answer("{") == ""
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_partial_answer.py -v`
Expected: FAIL（`ImportError: cannot import name '_partial_answer'`）

- [ ] **Step 3：實作**

在 `backend/qa.py`（`stream_answer` 之前）新增：
```python
def _partial_answer(raw: str) -> str:
    """從半截 JSON 串流文字容錯解出當前 answer 欄位值（json_repair）。失敗回 ''。"""
    if not raw:
        return ""
    try:
        data = json_repair.loads(raw)
        if isinstance(data, dict):
            a = data.get("answer", "")
            return a if isinstance(a, str) else ""
    except Exception:
        pass
    return ""
```

- [ ] **Step 4：跑測試確認通過**

Run: `python -m pytest tests/backend/test_qa_partial_answer.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5：Commit**

```bash
git add backend/qa.py tests/backend/test_qa_partial_answer.py
git commit -m "feat(qa): _partial_answer incremental extractor for streaming"
```

---

### Task 3：`stream_answer` JSON-first 守門(A2)

**Files:**
- Modify: `backend/qa.py`（`stream_answer` 串流迴圈）
- Test: `tests/backend/test_qa_stream_jsonfirst.py`

**假設:** 常態是「prose→```json」(保留現行);新增分支只處理模型「直接吐 JSON、沒寫 prose」的情況,避免 raw `{"answer"` 被當 prose 串出。

- [ ] **Step 1：寫失敗測試**

Create `tests/backend/test_qa_stream_jsonfirst.py`:
```python
# tests/backend/test_qa_stream_jsonfirst.py
"""stream_answer：模型 JSON-first（沒寫 prose 直接吐 JSON）時，串流不得漏 raw JSON 鷹架。"""
import asyncio
from backend import qa


class _Chunk:
    def __init__(self, text):
        self.text = text
        self.candidates = []


class _AStream:
    def __init__(self, texts):
        self._texts = texts

    def __aiter__(self):
        self._it = iter(self._texts)
        return self

    async def __anext__(self):
        try:
            return _Chunk(next(self._it))
        except StopIteration:
            raise StopAsyncIteration


class _AioModels:
    def __init__(self, texts):
        self._texts = texts

    async def generate_content_stream(self, **kwargs):
        return _AStream(self._texts)


class _Aio:
    def __init__(self, texts):
        self.models = _AioModels(texts)


class _Client:
    def __init__(self, texts):
        self.aio = _Aio(texts)


def _collect(texts):
    client = _Client(texts)

    async def run():
        toks, done = [], None
        async for ev in qa.stream_answer(client, "stores/x", "問題", None):
            if ev["event"] == "token":
                toks.append(ev["data"]["text"])
            elif ev["event"] == "done":
                done = ev["data"]
        return toks, done

    return asyncio.run(run())


def test_json_first_streams_clean_answer_only(monkeypatch):
    # 模型直接吐 JSON（沒 prose），分塊到達
    toks, done = _collect(['{"answer": "政大有', '幾門課', '", "followup_suggestions": []}'])
    joined = "".join(toks)
    assert '{"answer"' not in joined and "```" not in joined
    assert "政大有" in joined and "幾門課" in joined


def test_prose_then_fence_unchanged(monkeypatch):
    # 常態：prose 在前、```json 在後 → 只串 prose、遇 ``` 截斷
    toks, done = _collect(['政大有幾門課程。\n\n', '```json\n{"answer":"政大有幾門課程。","followup_suggestions":[]}\n```'])
    joined = "".join(toks)
    assert "政大有幾門課程。" in joined
    assert "```" not in joined and '{"answer"' not in joined
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_stream_jsonfirst.py -v`
Expected: FAIL（`test_json_first_streams_clean_answer_only`——目前 JSON-first 會把 raw `{` 當 prose 串出）

- [ ] **Step 3：實作**

把 `backend/qa.py` 的 `stream_answer` 串流主體（`chunks=[]` 到 done yield）改為：
```python
    chunks: list = []
    raw = ""             # 累積原始文字
    emitted = 0          # 已吐出的乾淨字元數
    mode = None          # "prose" | "json"（首段非空白字判定一次）
    fence_done = False
    stream = await client.aio.models.generate_content_stream(
        model="gemini-2.5-flash",
        contents=contents,
        config=config,
    )
    async for chunk in stream:
        chunks.append(chunk)
        text = _visible_text_from_chunk(chunk)   # 只取非 thought part
        if not text:
            continue
        raw += text
        if mode is None:
            s = raw.lstrip()
            if not s:
                continue
            mode = "json" if s[0] == "{" else "prose"
        if mode == "json":
            # JSON-first：增量抽乾淨 answer 值，絕不串 raw JSON 鷹架
            ans = _partial_answer(raw)
            if len(ans) > emitted:
                yield {"event": "token", "data": {"text": ans[emitted:]}}
                emitted = len(ans)
        else:
            # 常態 prose-then-fence：串 ``` 之前的乾淨 prose
            if fence_done:
                continue
            idx = raw.find("```")
            if idx == -1:
                safe = max(emitted, len(raw) - 2)   # 留尾端 2 字緩衝，避免吐半截 ``
                if safe > emitted:
                    yield {"event": "token", "data": {"text": raw[emitted:safe]}}
                    emitted = safe
            else:
                if idx > emitted:
                    yield {"event": "token", "data": {"text": raw[emitted:idx]}}
                emitted = idx
                fence_done = True

    # prose 模式且全程無 fence → 補吐保留的尾端緩衝
    if mode == "prose" and not fence_done and emitted < len(raw):
        yield {"event": "token", "data": {"text": raw[emitted:]}}

    course_ids = extract_course_ids_from_chunks(chunks)
    yield {"event": "done", "data": {
        "course_ids": course_ids,
        "answer_text": raw,
    }}
```

- [ ] **Step 4：跑測試確認通過 + 既有 stream 測試不回歸**

Run: `python -m pytest tests/backend/test_qa_stream_jsonfirst.py tests/backend/test_qa_stream.py tests/backend/test_qa_chunks.py -v`
Expected: PASS

- [ ] **Step 5：Commit**

```bash
git add backend/qa.py tests/backend/test_qa_stream_jsonfirst.py
git commit -m "fix(qa): stream_answer JSON-first guard — never stream raw JSON scaffolding"
```

---

### Task 4：`QA_MODE` 預設翻為 `stream`(C)

**Files:**
- Modify: `backend/main.py:41`
- Test: `tests/backend/test_qa_mode_default.py`

- [ ] **Step 1：寫失敗測試**

Create `tests/backend/test_qa_mode_default.py`:
```python
# tests/backend/test_qa_mode_default.py
"""QA_MODE 未設時預設走 2.5 串流（stream），3.5 replay 為選項。"""
import importlib


def test_default_qa_mode_is_stream(monkeypatch):
    monkeypatch.delenv("QA_MODE", raising=False)
    monkeypatch.setenv("ALLOWED_ORIGIN", "*")
    import backend.main as m
    importlib.reload(m)
    assert m._QA_MODE == "stream"
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_mode_default.py -v`
Expected: FAIL（目前預設 `"replay"`）

- [ ] **Step 3：實作**

把 `backend/main.py:41`：
```python
_QA_MODE = os.environ.get("QA_MODE", "replay")          # replay(新 3.5+schema) | stream(舊 2.5 串流)
```
改為：
```python
_QA_MODE = os.environ.get("QA_MODE", "stream")          # stream(預設 2.5 串流強化) | replay(3.5+schema 選項)
```

- [ ] **Step 4：跑測試確認通過 + 後端全測不回歸**

Run: `python -m pytest tests/backend/ -q`
Expected: PASS（既有 stream-path 測試已 pin QA_MODE=stream，replay 測試自設 env，皆綠）

- [ ] **Step 5：Commit**

```bash
git add backend/main.py tests/backend/test_qa_mode_default.py
git commit -m "feat(api): default QA_MODE=stream (2.5 hardened); 3.5 replay opt-in"
```

---

### Task 5：前端 `createTypewriter` backlog 自適應節奏(B3)

**Files:**
- Modify: `frontend/app.js`（`createTypewriter` 的 `tick`）
- Test: `tests/frontend/typewriter-pace.test.mjs`

**假設:** `createTypewriter` 目前一次吐 1 字、固定 `TYPE_INTERVAL`。改為:backlog 大時一次吐多字(加速追),backlog 小時維持逐字。為可單測,抽出純函式 `drainCount(backlog)`。

- [ ] **Step 1：寫失敗測試**

Create `tests/frontend/typewriter-pace.test.mjs`:
```javascript
// tests/frontend/typewriter-pace.test.mjs
import { strict as assert } from "node:assert";
import test from "node:test";
import { drainCount } from "../../frontend/progressive-md.js";

test("backlog 小 → 一次吐 1 字（保留打字感）", () => {
  assert.equal(drainCount(1), 1);
  assert.equal(drainCount(20), 1);
});

test("backlog 中 → 加速", () => {
  assert.ok(drainCount(120) >= 2);
});

test("backlog 大 → 更快但有上限", () => {
  const c = drainCount(2000);
  assert.ok(c >= 4 && c <= 8);   // 有上限，不一口氣噴完（保留動感）
});
```

- [ ] **Step 2：跑測試確認失敗**

Run: `node --test tests/frontend/typewriter-pace.test.mjs`
Expected: FAIL（`drainCount` 未匯出）

- [ ] **Step 3：實作**

(a) 在 `frontend/progressive-md.js` 末尾新增並匯出：
```javascript
// 依未顯示 backlog 決定本 tick 吐幾字：backlog 小逐字(打字感)，大則加速追上生成（有上限）。
export function drainCount(backlog) {
  if (backlog <= 40) return 1;
  if (backlog <= 200) return 2;
  if (backlog <= 600) return 4;
  return 8;
}
```

(b) 在 `frontend/app.js` 頂部 import 加入 `drainCount`：
```javascript
import { createProgressiveRenderer, stripInProgressTable, drainCount } from "./progressive-md.js";
```

(c) 把 `createTypewriter` 的 `tick` 中：
```javascript
    if(buffer.length){
      // 一次吐一個字（CJK 友善；可一次吐 1 字維持中速觀感）
      shown += buffer[0];
      buffer = buffer.slice(1);
      scheduleRender();
    } else if(inputDone){
```
改為：
```javascript
    if(buffer.length){
      // backlog 自適應：buffer 大則一次吐多字追上生成，小則逐字（CJK 友善）
      const n = drainCount(buffer.length);
      shown += buffer.slice(0, n);
      buffer = buffer.slice(n);
      scheduleRender();
    } else if(inputDone){
```

- [ ] **Step 4：跑測試確認通過**

Run: `node --test tests/frontend/typewriter-pace.test.mjs && node --check frontend/app.js`
Expected: PASS + 無語法錯

- [ ] **Step 5：Commit**

```bash
git add frontend/progressive-md.js frontend/app.js tests/frontend/typewriter-pace.test.mjs
git commit -m "feat(frontend): backlog-adaptive typewriter pace (drainCount)"
```

---

### Task 6：前端 `createTypewriter` live 尾端渲染 healed markdown(B2)

**Files:**
- Modify: `frontend/app.js`（`createTypewriter` 的 `doRender` + `liveEl` 容器）

**假設:** 純前端視覺改動 → 依專案紀律,改完先截 PNG 給使用者確認再 commit(見 Task 8)。把 live 尾端從「純文字 + stripInProgressTable」改為「healed markdown」,讓 prose 粗體即時生效、表格仍 snap-in。

- [ ] **Step 1：改 `liveEl` 容器(移除 pre-wrap，改可容 HTML)**

在 `frontend/app.js` 的 `createTypewriter` 中，把：
```javascript
  const liveEl = document.createElement("span");
  liveEl.style.whiteSpace = "pre-wrap";   // 純文字尾端保留換行/空白（表格原始列逐字出現）
```
改為：
```javascript
  const liveEl = document.createElement("span");
  // live 尾端改渲染 healed markdown（safeMarkdownPrefix 藏半截表格/未閉合粗體），故不再用 pre-wrap 純文字
```

- [ ] **Step 2：改 `doRender` 的 live 尾端**

把 `frontend/app.js` 的 `createTypewriter` → `doRender` 中：
```javascript
    // live 尾端：藏進行中表格(不露 raw |)，prose 照樣逐字；表格收尾後由 commit 渲染成 HTML
    liveEl.textContent = stripInProgressTable(shown.slice(committedLen));
```
改為：
```javascript
    // live 尾端：渲染 healed markdown（safeMarkdownPrefix 藏半截表格/未閉合粗體）→ 表格 snap-in、不露 raw |、粗體即時
    liveEl.innerHTML = renderSafeMarkdown(shown.slice(committedLen));
```

- [ ] **Step 3：語法煙霧測試**

Run: `node --check frontend/app.js`
Expected: 無輸出

- [ ] **Step 4：Commit（截圖確認後，見 Task 8 一併）**

先不 commit，待 Task 7 接好、Task 8 e2e/截圖確認後一起。（或單獨 commit:`git commit -m "feat(frontend): typewriter live tail renders healed markdown"`）

---

### Task 7：前端 `/qa/stream` token 改餵 `createTypewriter`(B1, B4)

**Files:**
- Modify: `frontend/app.js`（`askQuestion` 的 `token` 事件 handler）

**假設:** 串流路徑原本 token 丟給 `createProgressiveRenderer`(拿 chunk 即渲染→一坨一坨)。改餵 `createTypewriter`(定速緩衝)→ 平滑。done 的 `!tw`/`tw.finish` 分支沿用(createTypewriter 已相容 `push`/`finish`)。

- [ ] **Step 1：改 token handler 建立並餵 createTypewriter**

把 `frontend/app.js` 的 `askQuestion` 中 `es.addEventListener("token", ...)`：
```javascript
  es.addEventListener("token", (ev)=>{
    let d; try{ d = JSON.parse(ev.data); }catch(_){ return; }
    if(!(d && typeof d.text === "string")) return;
    if(!firstEvent){
      firstEvent = true;
      stage.stop();                 // 停階段 loader
      ans.innerHTML = "";           // 清掉 loader
      // 漸進 markdown：每 token rAF 節流重渲染累積緩衝 → 表格/粗體/標題隨完成即現
      tw = createProgressiveRenderer({ render: (buf)=> ans.innerHTML = renderSafeMarkdown(buf) });
    }
    tw.push(d.text);
  });
```
改為：
```javascript
  es.addEventListener("token", (ev)=>{
    let d; try{ d = JSON.parse(ev.data); }catch(_){ return; }
    if(!(d && typeof d.text === "string")) return;
    if(!firstEvent){
      firstEvent = true;
      stage.stop();                 // 停階段 loader
      // 定速緩衝打字機：Gemini 一坨一坨的 chunk 進 buffer，逐字平穩放出 → 不再突兀（smoothStream 模式）
      tw = createTypewriter(ans);
      tw.start();
    }
    tw.push(d.text);                // 餵 buffer（非立即渲染）
  });
```

- [ ] **Step 2：確認 done 分支相容**

確認 `done` handler：`tw` 存在時走 `tw.finish(renderFinal)`（createTypewriter 的 `finish(cb)` 會吐完 buffer→最終渲染→cb）。`!tw`（沒收到 token）走既有 replay 重播分支。**不需改 done。**

- [ ] **Step 3：移除未使用的 `createProgressiveRenderer` import**

`createProgressiveRenderer` 已無用(Task 7 改用 createTypewriter),`stripInProgressTable` 也已無用(Task 6 改用 renderSafeMarkdown)。把 import 改為只留 `drainCount`：
```javascript
import { drainCount } from "./progressive-md.js";
```
（`createTypewriter` 定義在 app.js 內,不需 import。`progressive-md.js` 內的 `createProgressiveRenderer`/`stripInProgressTable` 函式**不刪**——僅移除 app.js 的 import；其單元測試仍綠,日後清理。）

- [ ] **Step 4：語法煙霧測試**

Run: `node --check frontend/app.js && node --test "tests/frontend/**/*.test.mjs"`
Expected: 語法 OK；既有前端測試全綠

- [ ] **Step 5：Commit**

```bash
git add frontend/app.js
git commit -m "feat(frontend): stream tokens feed createTypewriter (smooth paced buffer)"
```

---

### Task 8：e2e 真後端驗證 — 無 JSON 外洩 + 平滑 + 表格

**Files:**
- Modify: `frontend/e2e/qa.spec.ts`（新增斷言）

**前置:** 本機起後端 `QA_MODE=stream GEMINI_API_KEY=… FILE_SEARCH_STORE_NAME=… ALLOWED_ORIGIN=* python -m uvicorn backend.main:app`,前端靜態服務。

- [ ] **Step 1：新增「無 JSON 鷹架」斷言**

在 `frontend/e2e/qa.spec.ts` 既有 Q&A 流程,問答完成後對 `[data-testid="qa-answer"]` 文字加：
```typescript
const answerText = await page.locator('[data-testid="qa-answer"]').innerText();
expect(answerText).not.toContain('```json');
expect(answerText).not.toContain('{"answer"');
expect(answerText).not.toContain('followup_suggestions');
await expect(page.locator('[data-testid="qa-citations"]')).toBeVisible();
```

- [ ] **Step 2：跑 e2e（真後端，QA_MODE=stream）**

Run: `cd frontend && npx playwright test e2e/qa.spec.ts`
Expected: PASS

- [ ] **Step 3：截圖給使用者確認(前端視覺紀律)**

擷取問答進行中(打字平滑、表格 snap-in)與完成畫面 PNG,交付使用者確認觀感後再續。

- [ ] **Step 4：Commit（含 Task 6 的視覺改動）**

```bash
git add frontend/app.js frontend/e2e/qa.spec.ts
git commit -m "test(qa): e2e asserts no JSON leak on 2.5 stream; visual confirmed"
```

---

### Task 9：收尾 — 全測試 + code review + 品質 + 文件

**Files:** 無新增（驗證 + 文件）

- [ ] **Step 1：全測試綠**

Run: `python -m pytest tests/backend/ -q && node --test "tests/frontend/**/*.test.mjs"`
Expected: 全 PASS

- [ ] **Step 2：superpowers code review**

invoke `superpowers:requesting-code-review` 對本分支 diff(`f611dbb..HEAD`)做審查;依 `superpowers:receiving-code-review` 處理回饋。

- [ ] **Step 3：品質掃描**

跑 `/simplify` 對改動檔做 reuse/簡化/效率/altitude 清理,僅採安全項。

- [ ] **Step 4：verification-before-completion**

invoke `superpowers:verification-before-completion`:貼出「真實 e2e 串流中+最終皆無 JSON 外洩 + 平滑觀感 + 全測試綠」的實際輸出證據,才宣稱完成。

- [ ] **Step 5：更新交接文件**

更新 `CLAUDE.md`(QA_MODE 預設 stream=2.5 強化、3.5 replay 選項與 503 緣由、平滑打字機=定速緩衝)與 `HANDOFF.md`(Session 5)。Commit：
```bash
git add CLAUDE.md HANDOFF.md
git commit -m "docs: Session 5 交接 — /qa 留 2.5 JSON 強化 + 平滑串流打字機"
```

---

## 範圍邊界（本計畫明確不做）

- `/recommend`（另議）。
- 3.5 當預設（parked，`QA_MODE=replay` 選項;stash 保留 `stream_answer_structured`/thinking）。
- Layer 4「sentinel 換格式」（未來,需重驗 grounding #15）。
- 刪除 `createProgressiveRenderer` 函式本身（僅移除 app.js import;函式與其測試保留,日後清理）。

## 回退

- 開發在 `feat/qa-hardening-smooth-stream`,master 不動。
- `QA_MODE=replay` 切到 3.5 選項;`git checkout master` 即回現狀。
- 部署層 `git revert` / 重部署 master。

# Q&A Gemini 3.5-flash 結構化串流遷移 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `/qa/stream` 預設改為 Gemini 3.5-flash 原生結構化串流（response_schema + file_search + thinking_level=low），治本 Session 8 幻覺/citation bug；保留 2.5 串流為 env 可切 fallback。

**Architecture:** qa.py 新增 `stream_answer_structured`（沿用既有 `_partial_answer`/`_visible_text_from_chunk`/`_grounding_course_ids_from_chunk`/`build_qa_contents`/`finalize_qa_answer`，僅換模型與 config）。main.py 用既有 `_QA_MODE` 加新值 `stream35`（新預設）做 dispatch；SSE 對前端 `token`/`done` 合約不變 → 前端零改。

**Tech Stack:** Python 3.11 / FastAPI / google-genai 2.7.0 / sse-starlette / pytest。分支 `feat/qa-gemini-3.5`。

---

## File Structure

- `backend/qa.py` — 新增 `stream_answer_structured`；`answer_question_structured` 加 `thinking_level="low"`。
- `backend/main.py` — `_QA_MODE` 預設/驗證更新；import 新函式；`/qa/stream` 與 POST `/qa` 的生成器選擇。
- `tests/backend/test_qa_stream_structured.py` — 新增（串流結構化單元測試）。
- `tests/backend/test_qa_structured.py` — 補 `thinking_level` 斷言（既有檔，若無則於 test_qa_stream_structured 一併測）。
- `tests/backend/test_qa_mode_stream35.py` — 新增（dispatch 預設與選路）。
- `CLAUDE.md` — #12 / #15 / 技術棧表更新。
- `scripts/eval_qa_10.py` — 新增（10 題驗收 harness，gitignored 不 commit）。

---

### Task 1: `stream_answer_structured`（qa.py 3.5 結構化串流）

**Files:**
- Modify: `backend/qa.py`（在 `stream_answer` 之後新增函式）
- Test: `tests/backend/test_qa_stream_structured.py`

- [ ] **Step 1: 寫失敗測試**

建立 `tests/backend/test_qa_stream_structured.py`：

```python
# tests/backend/test_qa_stream_structured.py
"""stream_answer_structured：3.5 結構化串流——逐 chunk 從部分 JSON 增量抽 answer 值 yield token，
末尾 done 帶 grounding course_ids + 原始 JSON 全文。mock client.aio.models.generate_content_stream。"""
import pytest
from types import SimpleNamespace
from unittest.mock import AsyncMock
from backend.qa import stream_answer_structured


def _chunk(text, course_id=None):
    # content=None → _visible_text_from_chunk 退回 chunk.text；grounding 用 title 的「課程代號:」regex 兜底
    if course_id:
        rc = SimpleNamespace(custom_metadata=None, title=f"課程代號: {course_id}", text="", uri=None)
        gm = SimpleNamespace(grounding_chunks=[SimpleNamespace(retrieved_context=rc)])
        cands = [SimpleNamespace(grounding_metadata=gm, content=None)]
    else:
        cands = [SimpleNamespace(grounding_metadata=None, content=None)]
    return SimpleNamespace(text=text, candidates=cands)


async def _aiter(items):
    for it in items:
        yield it


def _fake_client(chunks):
    aio = SimpleNamespace(models=SimpleNamespace(
        generate_content_stream=AsyncMock(return_value=_aiter(chunks))))
    return SimpleNamespace(aio=aio)


@pytest.mark.asyncio
async def test_structured_stream_yields_incremental_answer_then_done():
    # JSON 分段串流：token 只吐 answer 值（不含 JSON 鷹架）
    chunks = [
        _chunk('{"answer": "政治學'),
        _chunk(' 很棒",'),
        _chunk(' "followup_suggestions": []}', course_id="000211012"),
    ]
    client = _fake_client(chunks)
    events = [ev async for ev in stream_answer_structured(client, "store", "問政治學", None)]

    tokens = "".join(e["data"]["text"] for e in events if e["event"] == "token")
    assert tokens == "政治學 很棒"
    assert all('"answer"' not in e["data"]["text"] for e in events if e["event"] == "token")

    done = [e for e in events if e["event"] == "done"]
    assert len(done) == 1
    assert done[0]["data"]["course_ids"] == ["000211012"]
    assert '"answer"' in done[0]["data"]["answer_text"]   # 原始 JSON 全文留給 finalize/parse


@pytest.mark.asyncio
async def test_structured_stream_config_uses_schema_and_low_thinking():
    chunks = [_chunk('{"answer": "x", "followup_suggestions": []}')]
    client = _fake_client(chunks)
    _ = [ev async for ev in stream_answer_structured(client, "store", "q", None)]
    call = client.aio.models.generate_content_stream.await_args
    assert call.kwargs["model"] == "gemini-3.5-flash"
    cfg = call.kwargs["config"]
    assert cfg.response_schema is not None
    assert cfg.response_mime_type == "application/json"
    assert cfg.thinking_config is not None          # thinking_level=low 已設（加速且 grounding 不掉）
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_stream_structured.py -q`
Expected: FAIL（`ImportError: cannot import name 'stream_answer_structured'`）

- [ ] **Step 3: 實作（qa.py 在 `stream_answer` 函式之後新增）**

```python
async def stream_answer_structured(
    client: genai.Client,
    store_name: str,
    question: str,
    history: Optional[list] = None,
    model: str = "gemini-3.5-flash",
):
    """3.5 結構化串流：file_search + response_schema + streaming（thinking_level=low）。

    串流 chunk 是「合法部分 JSON」；用 _partial_answer 增量抽 answer 值 yield token（不串 JSON 鷹架）。
    done 帶 grounding course_ids（來自 grounding_metadata，3.5 穩定）+ 原始 JSON 全文 answer_text。
    citations / 防幻覺覆寫由呼叫端 finalize_qa_answer 處理（與 stream_answer 同合約）。
    ⚠️ 不可加 include_thoughts；thinking_level=low 已實測不掉 grounding（3.5 GA，非 preview 的 nil bug）。
    """
    contents = build_qa_contents(question, history, template=_PROMPT_TEMPLATE_STRUCTURED)
    config = types.GenerateContentConfig(
        system_instruction=_SYSTEM_INSTRUCTION,
        temperature=0.2,
        top_p=0.95,
        max_output_tokens=4096,
        response_mime_type="application/json",
        response_schema=_QA_RESPONSE_SCHEMA,
        thinking_config=types.ThinkingConfig(thinking_level="low"),
        tools=[
            types.Tool(
                file_search=types.FileSearch(
                    file_search_store_names=[store_name],
                    top_k=5,
                )
            )
        ],
    )
    raw = ""
    emitted = 0
    course_ids: list[str] = []
    stream = await client.aio.models.generate_content_stream(
        model=model, contents=contents, config=config,
    )
    async for chunk in stream:
        text = _visible_text_from_chunk(chunk)
        if text:
            raw += text
            ans = _partial_answer(raw)
            if len(ans) > emitted:
                yield {"event": "token", "data": {"text": ans[emitted:]}}
                emitted = len(ans)
        course_ids.extend(_grounding_course_ids_from_chunk(chunk))
    course_ids = list(dict.fromkeys(course_ids))
    yield {"event": "done", "data": {"course_ids": course_ids, "answer_text": raw}}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `python -m pytest tests/backend/test_qa_stream_structured.py -q`
Expected: PASS（2 passed）

- [ ] **Step 5: Commit**

```bash
git add backend/qa.py tests/backend/test_qa_stream_structured.py
git commit -m "feat(qa): stream_answer_structured——3.5 結構化串流(file_search+response_schema+thinking_level=low)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: `answer_question_structured` 加 `thinking_level="low"`（POST/replay 路徑一致加速）

**Files:**
- Modify: `backend/qa.py`（`answer_question_structured` 的 config）
- Test: `tests/backend/test_qa_structured.py`（補一條斷言；若無則加在 test_qa_stream_structured.py）

- [ ] **Step 1: 寫失敗測試**

在 `tests/backend/test_qa_stream_structured.py` 末尾新增：

```python
def test_answer_question_structured_sets_low_thinking(monkeypatch):
    from types import SimpleNamespace
    import backend.qa as qa
    captured = {}

    def fake_generate_content(model, contents, config):
        captured["config"] = config
        return SimpleNamespace(parsed={"answer": "x", "followup_suggestions": []},
                               text='{"answer":"x","followup_suggestions":[]}', candidates=[])

    client = SimpleNamespace(models=SimpleNamespace(generate_content=fake_generate_content))
    qa.answer_question_structured(client, "store", "q", history=None)
    assert captured["config"].thinking_config is not None
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_stream_structured.py::test_answer_question_structured_sets_low_thinking -q`
Expected: FAIL（`AttributeError`/`None`，因現有 config 未設 thinking_config）

- [ ] **Step 3: 實作（`answer_question_structured` 的 `types.GenerateContentConfig(...)` 內加一行）**

在 `response_schema=_QA_RESPONSE_SCHEMA,` 之後加：

```python
        thinking_config=types.ThinkingConfig(thinking_level="low"),
```

並把該函式 docstring 內「勿設顯式 thinking_config」更新為：
```python
    """...（前略）。thinking_level=low 加速且 3.5 GA 實測 grounding 不掉；勿用 include_thoughts。"""
```

- [ ] **Step 4: 跑測試確認通過**

Run: `python -m pytest tests/backend/test_qa_stream_structured.py -q`
Expected: PASS（3 passed）

- [ ] **Step 5: Commit**

```bash
git add backend/qa.py tests/backend/test_qa_stream_structured.py
git commit -m "feat(qa): answer_question_structured 加 thinking_level=low（POST/replay 路徑一致加速）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: main.py `_QA_MODE` dispatch（新增 `stream35` 並設為預設）

**Files:**
- Modify: `backend/main.py:21`（import）、`:44-48`（env）、`:279`（POST /qa）、`:381-382`（/qa/stream）
- Test: `tests/backend/test_qa_mode_stream35.py`

- [ ] **Step 1: 寫失敗測試**

建立 `tests/backend/test_qa_mode_stream35.py`：

```python
# tests/backend/test_qa_mode_stream35.py
"""QA_MODE 預設為 stream35（3.5 結構化串流）；/qa/stream 走 stream_answer_structured。"""
import importlib
import pytest
from fastapi.testclient import TestClient


def test_default_qa_mode_is_stream35(monkeypatch):
    monkeypatch.delenv("QA_MODE", raising=False)
    monkeypatch.setenv("ALLOWED_ORIGIN", "*")
    import backend.main as m
    importlib.reload(m)
    assert m._QA_MODE == "stream35"


def test_stream35_uses_structured_generator(monkeypatch):
    monkeypatch.setenv("QA_MODE", "stream35")
    monkeypatch.setenv("ALLOWED_ORIGIN", "*")
    import backend.main as m
    importlib.reload(m)

    used = {"structured": False, "plain": False}

    async def fake_structured(client, store, question, history=None, model="gemini-3.5-flash"):
        used["structured"] = True
        yield {"event": "token", "data": {"text": "嗨"}}
        yield {"event": "done", "data": {"course_ids": [], "answer_text":
               '{"answer":"嗨","followup_suggestions":[]}'}}

    async def fake_plain(client, store, question, history=None):
        used["plain"] = True
        yield {"event": "done", "data": {"course_ids": [], "answer_text": "{}"}}

    monkeypatch.setattr(m, "stream_answer_structured", fake_structured)
    monkeypatch.setattr(m, "stream_answer", fake_plain)

    with TestClient(m.app).stream("GET", "/qa/stream?question=測試") as resp:
        body = "".join(c for c in resp.iter_text())
    assert used["structured"] is True
    assert used["plain"] is False
    assert "event: done" in body
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_mode_stream35.py -q`
Expected: FAIL（`assert _QA_MODE == "stream35"` → 現值 "stream"；reload 時 RuntimeError 拒收 "stream35"）

- [ ] **Step 3: 實作 main.py**

(a) `backend/main.py:21` 的 qa import 加入新函式：
```python
    answer_question, answer_question_structured, finalize_qa_answer,
    stream_answer, stream_answer_structured, parse_qa_response, classify_qa_error,
```
（依現有 import 區實際成員調整，務必把 `stream_answer_structured` 加進去。）

(b) `backend/main.py:44-47` 改為：
```python
_QA_MODE = os.environ.get("QA_MODE", "stream35")   # stream35(預設 3.5 結構化串流) | stream(2.5 串流 fallback) | replay(3.5 非串流)
if _QA_MODE not in {"replay", "stream", "stream35"}:
    raise RuntimeError(f"QA_MODE 必須是 'stream35' / 'stream' / 'replay'，收到 {_QA_MODE!r}")
```

(c) POST `/qa` 的 else 分支（`backend/main.py:279`）把生成器改為依模式選擇：
```python
        else:
            _gen = stream_answer if _QA_MODE == "stream" else stream_answer_structured
            answer_text, course_ids = "", []
            async for ev in _gen(_client, _STORE_NAME, req.question, history):
                if ev["event"] == "done":
                    course_ids = ev["data"].get("course_ids", []) or []
                    answer_text = ev["data"].get("answer_text", "") or ""
            parsed = parse_qa_response(answer_text)
            # ...（其餘 result 組裝不變）
```

(d) `/qa/stream` 的 else 分支（`backend/main.py:381-382`）：
```python
            else:
                _gen = stream_answer if _QA_MODE == "stream" else stream_answer_structured
                async for ev in _gen(_client, _STORE_NAME, question, history):
                    # ...（其餘 token/done 處理不變）
```

- [ ] **Step 4: 跑測試確認通過**

Run: `python -m pytest tests/backend/test_qa_mode_stream35.py -q`
Expected: PASS（2 passed）

- [ ] **Step 5: 跑全套後端確認無回歸**

Run: `python -m pytest tests/backend -q`
Expected: 全綠（既有 `test_qa_stream_replay` 等仍過；若有測試硬編 `_QA_MODE=="stream"` 預設假設，更新為 stream35）

- [ ] **Step 6: Commit**

```bash
git add backend/main.py tests/backend/test_qa_mode_stream35.py
git commit -m "feat(qa): QA_MODE 新增 stream35 並設為預設（/qa/stream + POST /qa dispatch 3.5 結構化串流）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 文件更新（CLAUDE.md）

**Files:**
- Modify: `CLAUDE.md`（#12、#15、技術棧表生成模型欄）

- [ ] **Step 1: 更新 CLAUDE.md**

- #12「生成模型固定 gemini-2.5-flash」→ 改為：「**Q&A 預設 `gemini-3.5-flash` 原生結構化串流（response_schema+file_search+thinking_level=low）**，可 `QA_MODE=stream` 切回 2.5 串流 fallback、`replay` 走 3.5 非串流；推薦仍 2.5（下一輪遷移）。**仍不可為提速換 flash-lite。**」
- #15「grounding 對 prompt 極敏感…勿關 Q&A 串流 thinking」→ 句尾補：「（**此『勿關 thinking』僅適用 2.5**；3.5 GA 上 `thinking_level=low` 實測 grounding 完整、citation 正確——見 spec 2026-06-08-qa-gemini-3.5）。」
- 技術棧表「RAG/生成模型」欄：標注 Q&A=3.5-flash 結構化串流、推薦=2.5-flash。

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE #12/#15 更新——Q&A 預設 3.5 結構化串流、thinking note 標注僅適用 2.5

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: 驗證關卡（10 題零幻覺 GO/NO-GO + Playwright MCP）

> 這是 GO/NO-GO 閘門。需 `GEMINI_API_KEY` + `FILE_SEARCH_STORE_NAME`（真後端）。harness 為一次性腳本，**gitignored 不 commit**。

**Files:**
- Create: `scripts/eval_qa_10.py`（gitignored）

- [ ] **Step 1: 建 10 題評測 harness**

`scripts/eval_qa_10.py`：對「實際端點」（起本機後端，`QA_MODE=stream35`）逐題打 `/qa/stream`，收集 answer + citations + no_match + 延遲；每題跑：
- **確定性硬性檢查**：答案/表格列出的每門課名都在 `courses_meta.json`（幻覺數＝0）；列課時 citations 非空且對應主打課；好答案未被誤覆寫成 NO_RESULTS_MESSAGE。
- **延遲**：記 first-token/total。
題目（第 3 題為多輪，需帶 session_id 串兩輪）：
1 傳院有沒有 AI 課｜2 我想了解資料科學相關課程｜3 想當 PM 要修什麼課→那金融呢｜4 法律系有哪些科技/智財相關的課｜5 有沒有適合零程式基礎的人的課｜6 商院有沒有永續/ESG 課｜7 教我寫一首詩｜8 政大宿舍怎麼申請｜9 有沒有量子重力的課｜10 外語學院有哪些跨文化/國際商務的課。

- [ ] **Step 2: 起後端並跑 harness**

```bash
QA_MODE=stream35 ALLOWED_ORIGIN='*' python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 &
PYTHONPATH=. python scripts/eval_qa_10.py
```
Expected（GO 門檻）：
- **10/10 零幻覺**（任一題出現非 meta 課名即 NO-GO）。
- 第 7 離題導回、第 8 邊界、第 9 誠實 no_match（不可編出量子重力課）。
- 無好答案被誤覆寫。
- 延遲 total 中位數 ≤ ~16s。

- [ ] **Step 3: Playwright MCP 端到端（代表題 1/3/9）**

起後端（port 8000）→ Playwright MCP 載入 `http://localhost:8000` → 切問答模式 → 打第 1、3、9 題 → 驗：答案串流出、citation 正確、**不被「查無資料」誤覆寫**、第 9 題誠實查無、0 console error。截圖存證。

- [ ] **Step 4: 判定**

GO → 進 Task 6。NO-GO → 記錄失敗題、回 Task 1-3 調整（或維持 `QA_MODE=stream` 2.5 不上線）。**未過不得合併。**

---

### Task 6: 收尾（全綠 → 合併 → 部署 → 刪分支）

**Files:** 無（git 操作）

- [ ] **Step 1: 全套測試 + code review**

Run: `python -m pytest tests/backend -q`（全綠）。用 superpowers:requesting-code-review 對本分支 diff 做 review，Critical/Important 修掉。

- [ ] **Step 2: 合併到 master 並部署**

```bash
git checkout master && git merge --no-ff feat/qa-gemini-3.5
git push origin master        # Railway 自動部署
```

- [ ] **Step 3: 線上煙霧驗證**

`curl https://nccu-course.up.railway.app/health` + Playwright 載入線上、打第 1/9 題確認 3.5 結構化串流正常、citation 正確、不被覆寫。

- [ ] **Step 4: 刪除 feature 分支（用完即清，勿積壓）**

```bash
git branch -d feat/qa-gemini-3.5
git push origin --delete feat/qa-gemini-3.5 2>/dev/null || true
```
（用 superpowers:finishing-a-development-branch 收尾。）

---

## Self-Review

- **Spec coverage**：stream_answer_structured(Task1)✓、thinking_level=low(Task1/2)✓、QA_MODE fallback dispatch(Task3)✓、finalize 沿用(Task3)✓、前端合約不變(Task3 驗證)✓、10 題 GO/NO-GO(Task5)✓、Playwright(Task5/6)✓、CLAUDE 更新(Task4)✓、刪分支(Task6)✓。
- **Placeholder scan**：各 step 均含完整 code / 指令 / 期望輸出，無 TBD。
- **Type consistency**：`stream_answer_structured(client, store_name, question, history=None, model="gemini-3.5-flash")` 在 Task1 定義、Task3 以相同簽章呼叫；done 事件 `{course_ids, answer_text}` 與 main.py done 處理一致；`_QA_MODE` 值集 `{replay, stream, stream35}` 全檔一致。

---

**Out of scope（本計畫不做）**：職涯推薦遷移 3.5（下一輪）；offcanvas/careers/分頁（另一條 brainstorm）；2.5 脆弱 citation 邏輯不另補（靠遷移治本）。

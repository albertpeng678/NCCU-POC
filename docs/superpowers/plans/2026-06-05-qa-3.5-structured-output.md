# Q&A 3.5-flash 結構化輸出 + 打字機回歸 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `/qa` 從「2.5 自由文字串流硬解 JSON」改為「3.5-flash 非串流 `response_schema` 結構化輸出」根治 JSON 外洩，前端最小手術重用 `createTypewriter` 重播逐字 + 表格 snap-in；舊路徑以 `QA_MODE` env 並存回退。

**Architecture:** 後端新增非串流 `answer_question_structured()`（generate_content + response_schema + file_search，無顯式 thinking_config，max_output_tokens=8192）；`main.py` 用 `QA_MODE` 切換整個 QA 堆疊（replay 新 / stream 舊）。前端 `/qa/stream` done 的 `!tw` 分支改用打字機重播；`createTypewriter` live 尾端用新 `stripInProgressTable` 藏進行中表格。

**Tech Stack:** Python 3.11 + FastAPI + google-genai 1.68 + json-repair；原生 JS（marked + DOMPurify）；pytest + Node .mjs 測試 + Playwright e2e。

**紀律：** 全程 Karpathy guidelines（外科手術式最小 diff、不過度設計、明列假設、每 task 可驗證成功標準）。每 task 結尾 commit，訊息結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。分支：`feat/qa-3.5-structured`（已建）。

**前置假設（已 probe 驗證）：** 非串流 3.5 + file_search + response_schema 回完整 JSON + 6 筆 grounding；勿設顯式 `thinking_config`；2.5 配 schema 會 400。

---

### Task 1：新增 json-repair 依賴

**Files:**
- Modify: `backend/requirements.txt`

- [ ] **Step 1：加依賴**

在 `backend/requirements.txt` 末尾加一行：
```
json-repair>=0.30
```

- [ ] **Step 2：安裝並驗證可 import**

Run: `pip install "json-repair>=0.30" && python -c "import json_repair; print(json_repair.loads('{\"a\":1,}'))"`
Expected: 印出 `{'a': 1}`（容錯解析 trailing comma 成功）

- [ ] **Step 3：Commit**

```bash
git add backend/requirements.txt
git commit -m "chore(qa): add json-repair for defensive structured-output parsing"
```

---

### Task 2：`parse_structured_response` — 三層容錯解析

**Files:**
- Modify: `backend/qa.py`（檔頭加 `import json_repair`；於 `parse_qa_response` 之後新增函式）
- Test: `tests/backend/test_qa_structured_parse.py`

- [ ] **Step 1：寫失敗測試**

Create `tests/backend/test_qa_structured_parse.py`：
```python
# tests/backend/test_qa_structured_parse.py
"""parse_structured_response：三層容錯（parsed → json.loads → json_repair → 剝鷹架）。"""
from backend.qa import parse_structured_response


class _Resp:
    def __init__(self, parsed=None, text=""):
        self.parsed = parsed
        self.text = text


def test_uses_response_parsed_when_dict():
    r = _Resp(parsed={"answer": "嗨", "followup_suggestions": ["a"]})
    out = parse_structured_response(r)
    assert out["answer"] == "嗨"
    assert out["followup_suggestions"] == ["a"]


def test_falls_back_to_json_loads_when_parsed_none():
    r = _Resp(parsed=None, text='{"answer": "你好", "followup_suggestions": ["x", "y"]}')
    out = parse_structured_response(r)
    assert out["answer"] == "你好"
    assert out["followup_suggestions"] == ["x", "y"]


def test_json_repair_salvages_trailing_comma():
    r = _Resp(parsed=None, text='{"answer": "修", "followup_suggestions": ["x",],}')
    out = parse_structured_response(r)
    assert out["answer"] == "修"
    assert out["followup_suggestions"] == ["x"]


def test_strips_fence_when_all_else_fails():
    # 完全壞掉、無法解析 → 至少剝掉 ```json 鷹架，絕不把鷹架當答案
    r = _Resp(parsed=None, text='```json\n{ this is not json at all ```')
    out = parse_structured_response(r)
    assert "```" not in out["answer"]
    assert out["followup_suggestions"] == []
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_structured_parse.py -v`
Expected: FAIL（`ImportError: cannot import name 'parse_structured_response'`）

- [ ] **Step 3：實作**

在 `backend/qa.py` 檔頭 import 區加 `import json_repair`。在 `parse_qa_response` 函式之後新增：
```python
def _strip_fences(text: str) -> str:
    """移除 ```json / ``` 鷹架，回傳內層文字（最終 fallback 用，確保鷹架絕不外洩）。"""
    t = re.sub(r"```(?:json)?", "", text or "")
    return t.replace("```", "").strip()


def parse_structured_response(response) -> dict:
    """從 response_schema 的 generate_content 回應取 {answer, followup_suggestions}。

    三層容錯：response.parsed(dict) → json.loads(text) → json_repair.loads(text)；
    全失敗才剝鷹架把文字當 answer（followups 空）。絕不讓 JSON 鷹架外洩。
    """
    parsed = getattr(response, "parsed", None)
    if isinstance(parsed, dict) and "answer" in parsed:
        return {
            "answer": str(parsed.get("answer", "")),
            "followup_suggestions": list(parsed.get("followup_suggestions", []) or []),
        }

    text = getattr(response, "text", None) or ""
    for loader in (json.loads, json_repair.loads):
        try:
            data = loader(text)
            if isinstance(data, dict) and "answer" in data:
                return {
                    "answer": str(data.get("answer", "")),
                    "followup_suggestions": list(data.get("followup_suggestions", []) or []),
                }
        except Exception:
            continue

    return {"answer": _strip_fences(text), "followup_suggestions": []}
```

- [ ] **Step 4：跑測試確認通過**

Run: `python -m pytest tests/backend/test_qa_structured_parse.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5：Commit**

```bash
git add backend/qa.py tests/backend/test_qa_structured_parse.py
git commit -m "feat(qa): parse_structured_response with 3-layer tolerant parsing"
```

---

### Task 3：`_PROMPT_TEMPLATE_STRUCTURED` + `build_qa_contents` 接受 template 參數

**Files:**
- Modify: `backend/qa.py`（新增精簡 template；`build_qa_contents` 加 `template` 參數）
- Test: `tests/backend/test_qa_contents.py`（既有，新增一個測試）

- [ ] **Step 1：寫失敗測試**

在 `tests/backend/test_qa_contents.py` 末尾加：
```python
def test_structured_template_has_no_json_instruction():
    from backend.qa import build_qa_contents, _PROMPT_TEMPLATE_STRUCTURED
    c = build_qa_contents("我想學資料科學", history=None, template=_PROMPT_TEMPLATE_STRUCTURED)
    last = c[-1]["parts"][0]["text"]
    assert "我想學資料科學" in last
    # 結構化模板不得再教模型輸出 JSON 區塊（schema 接管）
    assert "json" not in last.lower()
    assert "followup_suggestions" not in last
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_contents.py::test_structured_template_has_no_json_instruction -v`
Expected: FAIL（`ImportError: cannot import name '_PROMPT_TEMPLATE_STRUCTURED'`）

- [ ] **Step 3：實作**

在 `backend/qa.py` 的 `_PROMPT_TEMPLATE` 之後新增精簡模板（無 JSON 尾巴）：
```python
_PROMPT_TEMPLATE_STRUCTURED = """\
學生問題：{question}

請依知識庫課綱回答。版型：先寫 3-5 句充實具體的文字說明（點出課程內容、為何適合、難度/先修）；
若在列出/比較多門課程，接著輸出 Markdown 表格，欄位固定為「課程名稱 | 系所 | 重點」，分隔線每欄三個連字號（---），最多 6 列；
純概念題或只談一門課則用文字即可。粗體克制：只標最關鍵的課名與 1-2 個能力關鍵詞。最後一句總結或建議。
（引用知識庫中實際存在的課程，勿編造課名/課號/老師。）
"""
```

修改 `build_qa_contents` 簽名，加 `template` 參數（預設舊模板，向後相容）：
```python
def build_qa_contents(question: str, history: Optional[list] = None, template: str = _PROMPT_TEMPLATE) -> list:
```
並把函式內最後一行的 `_PROMPT_TEMPLATE.format(...)` 改為 `template.format(question=question)`：
```python
    contents.append({"role": "user", "parts": [{"text": template.format(question=question)}]})
```

- [ ] **Step 4：跑測試確認通過（含既有測試不回歸）**

Run: `python -m pytest tests/backend/test_qa_contents.py -v`
Expected: PASS（既有 3 個 + 新 1 個）

- [ ] **Step 5：Commit**

```bash
git add backend/qa.py tests/backend/test_qa_contents.py
git commit -m "feat(qa): add structured prompt template + build_qa_contents template param"
```

---

### Task 4：`answer_question_structured` — 非串流 3.5 + schema 主函式

**Files:**
- Modify: `backend/qa.py`（新增函式）
- Test: `tests/backend/test_qa_structured.py`

- [ ] **Step 1：寫失敗測試**

Create `tests/backend/test_qa_structured.py`：
```python
# tests/backend/test_qa_structured.py
"""answer_question_structured：非串流 generate_content + response_schema，回乾淨結構化資料。"""
from backend.qa import answer_question_structured


class _Resp:
    def __init__(self, parsed, candidates=None):
        self.parsed = parsed
        self.text = ""
        self.candidates = candidates or []


class _FakeModels:
    def __init__(self, resp):
        self._resp = resp
        self.last_kwargs = None

    def generate_content(self, **kwargs):
        self.last_kwargs = kwargs
        return self._resp


class _FakeClient:
    def __init__(self, resp):
        self.models = _FakeModels(resp)


def test_returns_clean_answer_and_followups():
    resp = _Resp(parsed={"answer": "資料科學課程介紹", "followup_suggestions": ["q1", "q2"]})
    client = _FakeClient(resp)
    out = answer_question_structured(client, "stores/x", "我想學資料科學", history=None, model="gemini-3.5-flash")
    assert out["answer"] == "資料科學課程介紹"
    assert out["followup_suggestions"] == ["q1", "q2"]
    assert "citations_course_ids" in out
    assert "latency_ms" in out


def test_passes_schema_and_model_to_generate_content():
    resp = _Resp(parsed={"answer": "a", "followup_suggestions": []})
    client = _FakeClient(resp)
    answer_question_structured(client, "stores/x", "q", history=None, model="gemini-3.5-flash")
    kw = client.models.last_kwargs
    assert kw["model"] == "gemini-3.5-flash"
    cfg = kw["config"]
    # 結構化輸出 + 無顯式 thinking_config（Probe B：三開會截斷/掉 grounding）
    assert cfg.response_mime_type == "application/json"
    assert cfg.response_schema is not None
    assert getattr(cfg, "thinking_config", None) is None
    assert cfg.max_output_tokens == 8192
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_structured.py -v`
Expected: FAIL（`ImportError: cannot import name 'answer_question_structured'`）

- [ ] **Step 3：實作**

在 `backend/qa.py` 的 `answer_question` 函式之後新增（重用既有 `extract_course_ids_from_chunks`，它讀 `response.candidates[].grounding_metadata`，非串流 response 同樣有 `.candidates`）：
```python
def answer_question_structured(
    client: genai.Client,
    store_name: str,
    question: str,
    history: Optional[list] = None,
    model: str = "gemini-3.5-flash",
) -> dict:
    """非串流：3.5 + file_search + response_schema 回乾淨結構化答案。

    回 {answer, followup_suggestions, citations_course_ids, latency_ms}。
    勿設顯式 thinking_config（Probe B：file_search+schema+thinking 三開會截斷/掉 grounding）。
    max_output_tokens=8192：thinking 會吃輸出預算，2048 在長答案會截斷。
    """
    t0 = time.monotonic()
    contents = build_qa_contents(question, history, template=_PROMPT_TEMPLATE_STRUCTURED)
    config = types.GenerateContentConfig(
        system_instruction=_SYSTEM_INSTRUCTION,
        temperature=0.2,
        top_p=0.95,
        max_output_tokens=8192,
        response_mime_type="application/json",
        response_schema=_QA_RESPONSE_SCHEMA,
        tools=[
            types.Tool(
                file_search=types.FileSearch(
                    file_search_store_names=[store_name],
                    top_k=5,
                )
            )
        ],
    )
    response = client.models.generate_content(model=model, contents=contents, config=config)
    parsed = parse_structured_response(response)
    citations_course_ids = extract_course_ids_from_chunks([response])
    latency_ms = int((time.monotonic() - t0) * 1000)
    return {
        "answer": parsed["answer"],
        "followup_suggestions": parsed["followup_suggestions"],
        "citations_course_ids": citations_course_ids,
        "latency_ms": latency_ms,
    }
```

並在 `backend/qa.py` 模組層（`_PROMPT_TEMPLATE_STRUCTURED` 附近）新增 schema 常數：
```python
_QA_RESPONSE_SCHEMA = {
    "type": "object",
    "properties": {
        "answer": {"type": "string"},
        "followup_suggestions": {"type": "array", "items": {"type": "string"}},
    },
    "required": ["answer", "followup_suggestions"],
}
```

- [ ] **Step 4：跑測試確認通過**

Run: `python -m pytest tests/backend/test_qa_structured.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5：Commit**

```bash
git add backend/qa.py tests/backend/test_qa_structured.py
git commit -m "feat(qa): answer_question_structured (non-stream 3.5 + response_schema)"
```

---

### Task 5：`_SYSTEM_INSTRUCTION` 瘦身 — 移除 JSON 輸出指示

**Files:**
- Modify: `backend/qa.py`（`_SYSTEM_INSTRUCTION` 最後一行）
- Test: `tests/backend/test_qa_structured.py`（加一個斷言）

**假設（Karpathy 明列）：** 只移除「JSON 只在 ```json 區塊」這一條；首段「先檢索鐵則」與表格格式指引**保留**（grounding 命脈 + answer 內容格式仍需要）。

- [ ] **Step 1：寫失敗測試**

在 `tests/backend/test_qa_structured.py` 末尾加：
```python
def test_system_instruction_keeps_retrieval_rule_drops_json_rule():
    from backend.qa import _SYSTEM_INSTRUCTION
    # grounding 命脈：先檢索鐵則必須留
    assert "先" in _SYSTEM_INSTRUCTION and "檢索" in _SYSTEM_INSTRUCTION
    # 不再教模型輸出 ```json 區塊（schema 接管結構）
    assert "```json" not in _SYSTEM_INSTRUCTION
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_structured.py::test_system_instruction_keeps_retrieval_rule_drops_json_rule -v`
Expected: FAIL（目前 `_SYSTEM_INSTRUCTION` 含 `嚴禁在說明文字裡輸出 JSON；JSON 只在指定的 ```json 區塊出現。`）

- [ ] **Step 3：實作**

在 `backend/qa.py` 的 `_SYSTEM_INSTRUCTION` 中，刪除最後一條 bullet：
```
- 嚴禁在說明文字裡輸出 JSON；JSON 只在指定的 ```json 區塊出現。
```
（其餘格式鐵則保留。）

- [ ] **Step 4：跑測試確認通過 + 全 qa 測試不回歸**

Run: `python -m pytest tests/backend/ -k qa -v`
Expected: PASS（既有 + 新增皆綠）

- [ ] **Step 5：Commit**

```bash
git add backend/qa.py tests/backend/test_qa_structured.py
git commit -m "refactor(qa): drop JSON-output rule from system instruction (schema owns structure)"
```

---

### Task 6：`main.py` — env 開關 + `POST /qa` replay 分支

**Files:**
- Modify: `backend/main.py`（env 區；import；`POST /qa`）
- Test: `tests/backend/test_qa_ephemeral_api.py`（既有 API 測試樣式；新增 replay 測試檔）
- Test: `tests/backend/test_qa_structured_api.py`

- [ ] **Step 1：寫失敗測試**

Create `tests/backend/test_qa_structured_api.py`：
```python
# tests/backend/test_qa_structured_api.py
"""POST /qa 在 QA_MODE=replay 下走 answer_question_structured，回乾淨答案、不含 JSON 鷹架。"""
import os
import importlib
import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def client_replay(monkeypatch):
    monkeypatch.setenv("QA_MODE", "replay")
    monkeypatch.setenv("GEMINI_QA_MODEL", "gemini-3.5-flash")
    monkeypatch.setenv("ALLOWED_ORIGIN", "*")
    import backend.main as m
    importlib.reload(m)

    def fake_structured(client, store, question, history=None, model="gemini-3.5-flash"):
        return {
            "answer": "**資料探勘** 是入門好課。",
            "followup_suggestions": ["這門課難嗎？"],
            "citations_course_ids": [],
            "latency_ms": 10,
        }
    monkeypatch.setattr(m, "answer_question_structured", fake_structured)
    return TestClient(m.app)


def test_post_qa_replay_returns_clean_answer(client_replay):
    resp = client_replay.post("/qa", json={"question": "推薦資料科學課程"})
    assert resp.status_code == 200
    data = resp.json()
    assert "資料探勘" in data["answer"]
    assert "```json" not in data["answer"]
    assert '{"answer"' not in data["answer"]
    assert data["followup_suggestions"] == ["這門課難嗎？"]
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_structured_api.py -v`
Expected: FAIL（`AttributeError: module 'backend.main' has no attribute 'answer_question_structured'`）

- [ ] **Step 3：實作**

在 `backend/main.py`：

(a) import 區（`from backend.qa import (...)`）加入 `answer_question_structured`。

(b) env 區（`_ALLOWED_ORIGIN` 附近）加：
```python
_QA_MODE = os.environ.get("QA_MODE", "replay")          # replay(新 3.5+schema) | stream(舊 2.5 串流)
_QA_MODEL = os.environ.get("GEMINI_QA_MODEL", "gemini-3.5-flash")  # 僅 replay 生效，須 3-series
```

(c) `POST /qa`：把第 237-249 行的 Gemini 呼叫與持久化改成依模式分支。將
```python
    # Call Gemini
    result = None
    error = None
    try:
        result = answer_question(_client, _STORE_NAME, req.question, prev_interaction_id)
    except Exception as e:
        error = e
        sentry_sdk.capture_exception(e)

    # Persist turn (Phase 1)
    turn_id = await insert_turn(pool, session_id, turn_number, req.question, result, error)
    if result and result.get("interaction_id"):
        await bump_session(pool, session_id, result["interaction_id"])
```
改為：
```python
    # Call Gemini（QA_MODE 切換新舊堆疊）
    result = None
    error = None
    try:
        if _QA_MODE == "replay":
            history = []
            if req.session_id:
                turns = await get_session_turns(pool, session_id)
                history = build_history_from_turns(turns)
            result = await asyncio.to_thread(
                answer_question_structured, _client, _STORE_NAME, req.question, history, _QA_MODEL
            )
        else:
            result = answer_question(_client, _STORE_NAME, req.question, prev_interaction_id)
    except Exception as e:
        error = e
        sentry_sdk.capture_exception(e)

    # Persist turn (Phase 1)
    turn_id = await insert_turn(pool, session_id, turn_number, req.question, result, error)
    if result:
        # replay 無 interaction_id → bump 空字串推進 turn_count；stream 沿用 interaction_id
        await bump_session(pool, session_id, result.get("interaction_id") or "")
```

- [ ] **Step 4：跑測試確認通過**

Run: `python -m pytest tests/backend/test_qa_structured_api.py -v`
Expected: PASS（1 passed）

- [ ] **Step 5：Commit**

```bash
git add backend/main.py tests/backend/test_qa_structured_api.py
git commit -m "feat(api): POST /qa replay branch uses answer_question_structured (QA_MODE)"
```

---

### Task 7：`main.py` — `/qa/stream` replay 分支（stage + 單一 done）

**Files:**
- Modify: `backend/main.py`（`qa_stream` 的 `event_gen`）
- Test: `tests/backend/test_qa_stream_replay.py`

- [ ] **Step 1：寫失敗測試**

Create `tests/backend/test_qa_stream_replay.py`：
```python
# tests/backend/test_qa_stream_replay.py
"""/qa/stream 在 QA_MODE=replay：送 stage 事件 + 單一 done(乾淨 answer)，不逐 token、不含鷹架。"""
import importlib
import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def client_replay(monkeypatch):
    monkeypatch.setenv("QA_MODE", "replay")
    monkeypatch.setenv("GEMINI_QA_MODEL", "gemini-3.5-flash")
    monkeypatch.setenv("ALLOWED_ORIGIN", "*")
    import backend.main as m
    importlib.reload(m)

    def fake_structured(client, store, question, history=None, model="gemini-3.5-flash"):
        return {
            "answer": "**統計學** 是資料科學的地基。",
            "followup_suggestions": ["先修需求？"],
            "citations_course_ids": [],
            "latency_ms": 5,
        }
    monkeypatch.setattr(m, "answer_question_structured", fake_structured)
    return TestClient(m.app)


def test_stream_replay_emits_stage_and_done(client_replay):
    with client_replay.stream("GET", "/qa/stream?question=推薦課程") as resp:
        body = "".join(chunk for chunk in resp.iter_text())
    assert "event: stage" in body          # 立即 stage 事件（防前端 guard 誤觸 fallback）
    assert "event: done" in body
    assert "統計學" in body
    assert "```json" not in body
    assert "event: token" not in body       # replay 不逐 token
```

- [ ] **Step 2：跑測試確認失敗**

Run: `python -m pytest tests/backend/test_qa_stream_replay.py -v`
Expected: FAIL（目前 `/qa/stream` 只走 `stream_answer`，無 stage 事件、無 replay 分支）

- [ ] **Step 3：實作**

在 `backend/main.py` 的 `qa_stream` 內，把 `event_gen` 改為依 `_QA_MODE` 分支。將既有：
```python
    async def event_gen():
        meta = load_courses_meta()
        result_dict = None
        error = None
        try:
            async for ev in stream_answer(_client, _STORE_NAME, question, history):
                ...（既有 token/done 迴圈）...
        except Exception as e:
            ...
        if result_dict:
            await insert_turn(pool, session_id, turn_number, question, result_dict, error)
            await bump_session(pool, session_id, "")
```
改為（在 try 內最前面分支；replay 走新邏輯，stream 維持既有迴圈）：
```python
    async def event_gen():
        meta = load_courses_meta()
        result_dict = None
        error = None
        try:
            if _QA_MODE == "replay":
                yield _sse("stage", {"label": "檢索課綱中…"})   # 立即觸發前端 firstEvent
                if await request.is_disconnected():
                    return
                result = await asyncio.to_thread(
                    answer_question_structured, _client, _STORE_NAME, question, history, _QA_MODEL
                )
                answer = result["answer"]
                followups = result["followup_suggestions"]
                citations = extract_citations(result["citations_course_ids"], meta)
                if not citations:
                    citations = extract_citations_by_name(answer, meta)
                if should_override_no_results(answer, citations):
                    answer = NO_RESULTS_MESSAGE
                    followups = []
                result_dict = {
                    "answer": answer,
                    "citations_course_ids": result["citations_course_ids"],
                    "followup_suggestions": followups,
                    "latency_ms": result["latency_ms"],
                }
                yield _sse("done", {
                    "answer": answer,
                    "citations": citations,
                    "followup_suggestions": followups,
                    "session_id": session_id,
                    "turn_number": turn_number,
                })
            else:
                async for ev in stream_answer(_client, _STORE_NAME, question, history):
                    if await request.is_disconnected():
                        return
                    if ev["event"] == "token":
                        yield _sse("token", ev["data"])
                    elif ev["event"] == "done":
                        parsed = parse_qa_response(ev["data"]["answer_text"])
                        citations = extract_citations(ev["data"]["course_ids"], meta)
                        answer = parsed["answer"]
                        followups = parsed["followup_suggestions"]
                        if not citations:
                            citations = extract_citations_by_name(answer, meta)
                        if should_override_no_results(answer, citations):
                            answer = NO_RESULTS_MESSAGE
                            followups = []
                        result_dict = {
                            "answer": answer,
                            "citations_course_ids": ev["data"]["course_ids"],
                            "followup_suggestions": followups,
                            "latency_ms": 0,
                        }
                        yield _sse("done", {
                            "answer": answer,
                            "citations": citations,
                            "followup_suggestions": followups,
                            "session_id": session_id,
                            "turn_number": turn_number,
                        })
        except Exception as e:
            error = e
            sentry_sdk.capture_exception(e)
            yield _sse("error", {"error_type": type(e).__name__, "message": str(e)})

        if result_dict:
            await insert_turn(pool, session_id, turn_number, question, result_dict, error)
            await bump_session(pool, session_id, "")
```

- [ ] **Step 4：跑測試確認通過 + stream 模式不回歸**

Run: `python -m pytest tests/backend/test_qa_stream_replay.py tests/backend/test_qa_stream.py -v`
Expected: PASS（replay 新測試 + 既有 stream 測試皆綠）

- [ ] **Step 5：Commit**

```bash
git add backend/main.py tests/backend/test_qa_stream_replay.py
git commit -m "feat(api): /qa/stream replay branch (stage + single done, no token)"
```

---

### Task 8：前端 `stripInProgressTable` 純函式 + 單測

**Files:**
- Modify: `frontend/progressive-md.js`（新增 export）
- Test: `tests/frontend/strip-table.test.mjs`

- [ ] **Step 1：寫失敗測試**

Create `tests/frontend/strip-table.test.mjs`（仿 `tests/frontend/progressive-md.test.mjs` 樣式）：
```javascript
// tests/frontend/strip-table.test.mjs
import { strict as assert } from "node:assert";
import test from "node:test";
import { stripInProgressTable } from "../../frontend/progressive-md.js";

test("純 prose 原樣回傳", () => {
  assert.equal(stripInProgressTable("這是一段文字"), "這是一段文字");
});

test("進行中表格(未收尾)→ 砍到表格列前", () => {
  const tail = "以下是課程：\n| 課程名稱 | 系所 |";
  assert.equal(stripInProgressTable(tail), "以下是課程：\n");
});

test("已收尾(空行)表格 → 原樣回傳交給 commit", () => {
  const tail = "| a | b |\n| --- | --- |\n| 1 | 2 |\n\n";
  assert.equal(stripInProgressTable(tail), tail);
});

test("空字串安全", () => {
  assert.equal(stripInProgressTable(""), "");
});
```

- [ ] **Step 2：跑測試確認失敗**

Run: `node --test tests/frontend/strip-table.test.mjs`
Expected: FAIL（`stripInProgressTable` 未匯出）

- [ ] **Step 3：實作**

在 `frontend/progressive-md.js` 末尾新增並匯出：
```javascript
// 從打字機 live 尾端移除「進行中的表格」(某行以 | 起頭且整段未以空行收尾)，
// 避免逐字打字時露出 raw `| 課程 |` 管線；表格收尾(空行)後交由 commit 邏輯渲染成 HTML。
export function stripInProgressTable(tail) {
  if (!tail) return tail;
  const endsClean = /\n[^\S\n]*\n[^\S\n]*$/.test(tail);
  if (endsClean) return tail;            // 已收尾 → 不動
  const m = tail.match(/(^|\n)[^\S\n]*\|/);
  if (!m) return tail;                   // 無表格列
  const lineStart = m.index + (m[1] === "\n" ? 1 : 0);
  return tail.slice(0, lineStart);
}
```

- [ ] **Step 4：跑測試確認通過**

Run: `node --test tests/frontend/strip-table.test.mjs`
Expected: PASS（4 tests）

- [ ] **Step 5：Commit**

```bash
git add frontend/progressive-md.js tests/frontend/strip-table.test.mjs
git commit -m "feat(frontend): stripInProgressTable helper for typewriter live tail"
```

---

### Task 9：前端 `createTypewriter` live 尾端套用 `stripInProgressTable`

**Files:**
- Modify: `frontend/app.js`（import；`createTypewriter` 的 `doRender`）

**假設：** 純前端視覺改動 → 依專案紀律，改完先截 PNG 給使用者確認再 commit（見 Task 11 e2e/截圖）。

- [ ] **Step 1：加 import**

在 `frontend/app.js` 第 3 行（既有 import）改為同時引入：
```javascript
import { createProgressiveRenderer, stripInProgressTable } from "./progressive-md.js";
```

- [ ] **Step 2：改 `doRender` 的 live 尾端**

在 `frontend/app.js` 的 `createTypewriter` → `doRender` 中，將：
```javascript
    liveEl.textContent = shown.slice(committedLen);
```
改為：
```javascript
    // live 尾端：藏進行中表格(不露 raw |)，prose 照樣逐字；表格收尾後由 commit 渲染成 HTML
    liveEl.textContent = stripInProgressTable(shown.slice(committedLen));
```

- [ ] **Step 3：語法/載入煙霧測試**

Run: `node --check frontend/app.js`
Expected: 無輸出（語法正確）

- [ ] **Step 4：Commit**

```bash
git add frontend/app.js
git commit -m "feat(frontend): hide in-progress table in typewriter live tail"
```

---

### Task 10：前端 `/qa/stream` done 的 `!tw` 分支改打字機重播

**Files:**
- Modify: `frontend/app.js`（`askQuestion` 的 `done` handler `!tw` 分支；新增 `stage` 事件監聽）

- [ ] **Step 1：加 `stage` 事件監聽（防 guard 誤觸 fallback）**

在 `frontend/app.js` 的 `askQuestion` 內、`es.addEventListener("token", ...)` 之前，加：
```javascript
  es.addEventListener("stage", ()=>{
    firstEvent = true;          // replay 模式無 token：靠 stage 事件取消首事件 guard、維持階段 loader
  });
```

- [ ] **Step 2：改 done 的 `!tw` 分支**

在 `frontend/app.js` 的 `es.addEventListener("done", ...)` 中，將：
```javascript
    if(!tw){ stage.stop(); renderFinal(); return; }   // 沒收到任何 token 就 done → 停階段、直接渲染
```
改為：
```javascript
    if(!tw){
      stage.stop();
      // replay 模式（無 token）：用打字機重播完整答案 → 逐字 + 表格 snap-in
      if(typeof data.answer === "string" && data.answer){
        const rtw = createTypewriter(ans);
        rtw.start();
        rtw.push(data.answer);
        rtw.finish(()=>{ attachCitesAndFollowups(bubble, data); finishQaTurn(); });
      } else {
        renderFinal();
      }
      return;
    }
```

- [ ] **Step 3：語法煙霧測試**

Run: `node --check frontend/app.js`
Expected: 無輸出

- [ ] **Step 4：Commit**

```bash
git add frontend/app.js
git commit -m "feat(frontend): typewriter replay on /qa/stream done (replay mode)"
```

---

### Task 11：e2e 真後端驗證 — 無 JSON 外洩

**Files:**
- Modify: `frontend/e2e/qa.spec.ts`（新增斷言）

**前置：** 本機起後端 `QA_MODE=replay GEMINI_QA_MODEL=gemini-3.5-flash ALLOWED_ORIGIN=* GEMINI_API_KEY=... FILE_SEARCH_STORE_NAME=... python -m uvicorn backend.main:app`，前端靜態服務。

- [ ] **Step 1：新增「無 JSON 鷹架」斷言**

在 `frontend/e2e/qa.spec.ts` 既有 Q1-Q5 流程中，問答完成後對答案區塊文字加斷言：
```typescript
const answerText = await page.locator('[data-testid="qa-answer"]').innerText();
expect(answerText).not.toContain('```json');
expect(answerText).not.toContain('{"answer"');
expect(answerText).not.toContain('followup_suggestions');
// citations 與 followups 有出現
await expect(page.locator('[data-testid="qa-citations"]')).toBeVisible();
```
（若既有 testid 名稱不同，依 `frontend/index.html` 實際 `data-testid` 調整。）

- [ ] **Step 2：跑 e2e（真後端）**

Run: `cd frontend && npx playwright test e2e/qa.spec.ts`
Expected: PASS；過程截圖無 JSON 鷹架。

- [ ] **Step 3：截圖給使用者確認（前端視覺紀律）**

擷取問答完成畫面 PNG（打字機 + 表格 snap-in + citations），交付使用者確認觀感後再繼續。

- [ ] **Step 4：Commit**

```bash
git add frontend/e2e/qa.spec.ts
git commit -m "test(qa): e2e asserts no JSON scaffolding leaks in replay mode"
```

---

### Task 12：收尾 — 全測試 + code review + 品質驗證

**Files:** 無新增（驗證 + 文件更新）

- [ ] **Step 1：全測試綠**

Run: `python -m pytest tests/backend/ -q && node --test tests/frontend/`
Expected: 全 PASS（含既有，無回歸）

- [ ] **Step 2：superpowers code review**

invoke `superpowers:requesting-code-review` 對本分支 diff 做審查；依 `superpowers:receiving-code-review` 處理回饋。

- [ ] **Step 3：品質掃描**

跑 `/simplify`（reuse/simplification/efficiency/altitude）對改動檔做品質清理，僅採安全項。

- [ ] **Step 4：verification-before-completion**

invoke `superpowers:verification-before-completion`：貼出「真實 e2e 無 JSON 外洩 + 全測試綠」的實際輸出證據，才宣稱完成。

- [ ] **Step 5：更新交接文件**

更新 `CLAUDE.md`（新增 QA_MODE/GEMINI_QA_MODEL、設計決策：3.5+schema 根治 JSON 外洩、串流掉 grounding 故走非串流）與 `HANDOFF.md`（Session 5 段）。Commit：
```bash
git add CLAUDE.md HANDOFF.md
git commit -m "docs: Session 5 交接 — /qa 遷 3.5 結構化輸出 + 打字機回歸"
```

---

## 範圍邊界（本計畫明確不做）

- `/recommend` 遷 3.5（另一輪，先 probe 延遲/品質）。
- 刪舊串流路徑、合併渲染器（A 上線穩定後另一輪清理）。
- code-fence 防呆（YAGNI，Q&A 用不到）。

## 回退

- 開發在 `feat/qa-3.5-structured`，master 不動。
- env 成套切：`QA_MODE=stream`（舊 2.5 串流）即回退，不改 code。
- 部署層 `git revert` merge / 重部署 master。
